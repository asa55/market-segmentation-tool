import React from "react"
import contentvisualStyles from "./content.visual.module.css"

const ContentVisual = () => (
  <section className={contentvisualStyles.contentVisual}>
    <div>
        
    </div>
  </section>
)

export default ContentVisual
